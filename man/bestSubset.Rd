% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bestSubset.R
\name{bestSubset}
\alias{bestSubset}
\title{Best Subset Selection for Logistic Regression}
\usage{
bestSubset(
  X,
  y,
  max_variables = NULL,
  top_n = 5,
  metric = c("auc", "accuracy", "deviance"),
  cross_validation = FALSE,
  cv_folds = 5,
  cv_repeats = 1,
  cv_seed = NULL,
  na.action = na.fail
)
}
\arguments{
\item{X}{A numeric matrix or data frame of predictor variables (n x p).
Categorical variables are not supported - use model.matrix() to create
dummy variables first.}

\item{y}{A numeric vector of binary outcomes containing exactly 0 and 1 values.
Other binary formats require conversion}

\item{max_variables}{Maximum number of variables to consider in subsets.
If NULL (default), considers all variables. Values > 20 will trigger
a warning about computational complexity.}

\item{top_n}{Number of top models to return (default: 5, max: 10)}

\item{metric}{Selection metric. One of "accuracy", "auc" (default), or "deviance".
For "accuracy" and "auc", higher values indicate better models.
For "deviance", lower values indicate better model fit (better match to data).}

\item{cross_validation}{Logical indicating whether to use cross-validation (default: FALSE)}

\item{cv_folds}{Number of cross-validation folds (default: 5)}

\item{cv_repeats}{Number of cross-validation repeats (default: 1)}

\item{cv_seed}{Random seed for cross-validation reproducibility (default: NULL)}

\item{na.action}{How to handle missing values. One of na.fail (default), na.omit, or na.exclude}
}
\value{
A list with class "bestSubset" containing:
\describe{
\item{models}{Data frame with results for top models}
\item{best_model}{List with detailed information about the best model}
\item{call_info}{Information about the function call and data}
\item{call}{The original function call}
}
}
\description{
Performs best subset selection for logistic regression to find
the optimal subset of predictors based on various metrics.
}
\examples{
# Generate sample data (small for fast execution)
set.seed(123)
X <- matrix(rnorm(20*3), nrow=20, ncol=3)
colnames(X) <- paste0("X", 1:3)
y <- rbinom(20, 1, plogis(X[,1] + 0.5*X[,2]))

# Basic best subset selection
result <- bestSubset(X, y, max_variables=2, top_n=3)
print(result)
summary(result)

}
